/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Required for Cloudflare Pages deployment with @cloudflare/next-on-pages
  // See README for deployment details
};

module.exports = nextConfig;
